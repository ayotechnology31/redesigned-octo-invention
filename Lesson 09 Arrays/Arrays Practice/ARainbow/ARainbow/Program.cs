using System;

namespace ARainbow
{
	class Program
	{
		static void Main(string[] args)
		{
			//Type out the following code to first create and then print the given String array.
			//Something doesn't quite match up, fix the access calls to print out in true ROYGBIV order!

			String[] colors = { "Red", "Orange", "Yellow", "Green", "Blue", "Indigo", "Violet" };

			Console.WriteLine(colors[0]);
			Console.WriteLine(colors[1]);
			Console.WriteLine(colors[2]);
			Console.WriteLine(colors[3]);
			Console.WriteLine(colors[4]);
			Console.WriteLine(colors[5]);
			Console.WriteLine(colors[6]);
		}
	}
}
